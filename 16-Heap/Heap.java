import java.io.*;
import java.util.*;

public class Heap {
    
    private Node root;
    private ArrayList<Integer> heap = new ArrayList<Integer>();
    
    public Heap() {
	for (int i = 0; i < 10; i++) {
	    Random r = new Random();
	    heap.add(r.nextInt(10));
	}
    }

    public 
    
	
	
